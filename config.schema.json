{
  "pluginAlias": "Tiko",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Nom de la plateforme",
        "type": "string",
        "required": true,
        "default": "Tiko"
      },
      "login": {
        "title": "Login Tiko",
        "type": "string",
        "required": true
      },
      "password": {
        "title": "Mot de passe Tiko",
        "type": "string",
        "required": true
      },
      "endpoint": {
        "title": "Endpoint Tiko (facultatif)",
        "type": "string",
        "required": false,
        "default": "https://particuliers-tiko.fr"
      },
      "propertyId": {
        "title": "Identifiant de propriété (facultatif)",
        "type": "number",
        "required": false
      }
    }
  }
}
